<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Chi tiết Hành trình</title>
    <link rel="icon" href="/static/images/favicon2.png">

    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="/static/css/bootstrap.min.css">
    <!-- Custom Styles -->
    <link rel="stylesheet" href="/static/css/style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    <link rel="stylesheet" href="/static/css/main.css">
    <style>
        body {
            background-image: #fff !important;
        }

        /* General container styling */
        .journey-container {
            margin: 110px auto 50px auto;
            padding: 20px;
        }

        /* Journey title */
        .journey-title {
            font-size: 25px;
            font-weight: 700;
            text-transform: uppercase;
            color: #333;
            text-align: center;
            margin-bottom: 50px;
            position: relative;
        }

        /* Card styling for gates */
        .gate-card {
            width: 50%;
            background: #fff;
            border-radius: 12px;
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.15);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            padding: 20px;
            border: 1px solid transparent;
            margin: 0 auto 30px auto;
            position: relative;
            overflow: hidden;
        }

        /* Gradient effect for gates */
        .gate-card:before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            z-index: -1;
            opacity: 0.1;
        }

        /* Hover effect for gate cards */
        .gate-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 12px 30px rgba(0, 0, 0, 0.2);
            border-color: #39517c;
        }

        /* Gate title */
        .gate-title {
            font-size: 25px;
            font-weight: 700;
            color: #333;
            text-align: center;
        }

        /* Stage list container */
        .stage-list-container {
            text-align: center;
        }

        /* Stage card styling */
        .stage-card {
            width: 30%; /* Adjust width similar to gate */
            background: #fff;
            border-radius: 8px;
            padding: 20px;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
            margin: 30px auto 10px auto; /* Center the stage cards */
            border-left: 5px solid #519be4;
            transition: background-color 0.3s ease;
        }

        /* Stage hover effect */
        .stage-card:hover {
            border: 1px solid #044efc;
        }

        /* Stage title */
        .stage-title {
            font-size: 20px;
            font-weight: 600;
            color: #333;
        }

        .btn-primary {
            display: block;
            width: 100%;
            padding: 8px;
            border-radius: 15px;
            background-image: linear-gradient(45deg, #f1447d, #3278f1);
            border: none;
            color: #fff;
            font-size: 15px;
            font-weight: bold;
            margin-top: 20px;
            text-align: center;
            transition: background 0.3s ease, box-shadow 0.3s ease;
        }

        .btn-primary:hover {
            background: white;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2); /* Shadow on hover */
            border: 1px solid #d87a99;
            color: #333;
        }
        /* Decorative lines */
        .journey-title::after, .journey-title::before {
            content: '';
            display: block;
            width: 100px;
            height: 3px;
            background-color: #5589e3;
            margin: 15px auto;
        }
        .achievement-panel,
        .leaderboard-panel {
            position: -webkit-sticky; /* For Safari */
            position: sticky;
            top: 95px; /* The distance from the top of the viewport when it becomes sticky */
        }
        /* Styling for left panel - Achievement */
        .achievement-panel {
            background-color: #fff;
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.1);
            padding: 20px;
            border-left: 2px solid sandybrown; /* Chỉ áp dụng cho border trái */
            border-right: 2px solid sandybrown; /* Chỉ áp dụng cho border phải */
        }

        .achievement-panel h4 {
            font-weight: 700;
            font-size: 20px;
            color: #ed3535;
            text-align: center;
            margin: 30px 0 25px 0;
            font-family: 'Poppins', sans-serif;
        }

        .achievement-list 
        .daily-tasks {
            list-style: none;
            padding: 0;
        }

        .achievement-list li 
        .daily-tasks li {
            margin-bottom: 10px;
            font-size: 16px;
            color: #000000;
            background-color: #d9ecff;
            padding: 10px;
            border-radius: 8px;
        }
        .achievement-list,
        .daily-tasks {
            list-style-type: none;
            padding: 0;
        }

        .achievement-list li,
        .daily-tasks li {
            font-size: 16px;
            margin-bottom: 15px;
            color: #000000;
        }

        .upgrade-section {
            text-align: center;
            margin-top: 20px;
        }
        .upgrade-section h4 {
            font-size: 20px;
            font-weight: 700;
            color: #ed3535;
            text-align: center;
            margin: 30px 0 25px 0;
            font-family: 'Poppins', sans-serif;
        }

        .upgrade-section p {
            font-size: 16px;
            color: #000000;
            margin-bottom: 15px;
        }

        .btn-upgrade {
            background-color: #4487fa;
            border: none;
            color: #fff;
            padding: 10px 20px;
            border-radius: 20px;
            transition: background-color 0.3s ease;
        }

        .btn-upgrade:hover {
            background-color: #044efc;
        }
        /* Styling for right panel - Leaderboard */
        .leaderboard-panel {
            height: calc(100vh - 50px); /* Adjust height based on the viewport */
            overflow-y: auto; /* Enable scrolling within the panel if the content is taller */
            background-color: #fff;
            padding: 10px;
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.1);
            border-left: 2px solid rgb(238, 190, 0); /* Chỉ áp dụng cho border trái */
            border-right: 2px solid rgb(238, 190, 0); /* Chỉ áp dụng cho border phải */
        }

        .leaderboard-panel h4 {
            font-size: 20px;
            font-weight: 700;
            color: #ed3535;
            text-align: center;
            margin: 30px 0 25px 0;
            font-family: 'Poppins', sans-serif;
        }

        .leaderboard-table {
            width: 100%;
            text-align: center;
        }

        .leaderboard-table th, .leaderboard-table td {
            padding: 10px;
            border-bottom: 1px solid #e1e1e1;
        }

        .leaderboard-table th {
            background-color: #eeeeee;
        }

        .leaderboard-table td {
            font-size: 16px;
            color: #000000;
        }
    </style>
</head>
<body>
    <%- include('../partials/menu.ejs') %>
    <div class="container journey-container">
        <div class="row">
            <div class="col-md-3">
                <div class="achievement-panel">
                    <h4>Thành tích của bạn</h4>
                    <ul class="achievement-list">
                        <li>Điểm KN: 1800</li>
                        <li>Hành trình đã hoàn thành: 5</li>
                        <li>Chặng đã hoàn thành: 20</li>
                    </ul>
                    <hr>
                    
                    <!-- Nhiệm vụ hàng ngày -->
                    <h4>Nhiệm vụ hàng ngày</h4>
                    <ul class="daily-tasks">
                        <li><i class="fas fa-check-circle"></i> Hoàn thành 1 chặng</li>
                        <li><i class="fas fa-check-circle"></i> Học từ vựng mới: 5 từ</li>
                        <li><i class="fas fa-check-circle"></i> Thực hành 10 phút giao tiếp</li>
                    </ul>
                    <hr>
                    
                    <!-- Nâng cấp EasyTalk Pro -->
                    <div class="upgrade-section">
                        <h4>Nâng cấp EasyTalk Pro</h4>
                        <p>Nâng cấp để có trải nghiệm tốt hơn và truy cập không giới hạn các tính năng.</p>
                        <button class="btn btn-info btn-upgrade">Xem chi tiết</button>
                    </div>
                </div>
            </div>            
            <div class="col-md-6">
                <h2 class="journey-title">Hành trình: <%= journey.title %></h2>
                <div class="row justify-content-center">
                    <% if (journey.gates.length > 0) { %>
                        <% journey.gates.forEach(gate => { %>
                            <!-- Gate card -->
                            <div class="col-12 mb-3">
                                <div class="gate-card">
                                    <h5 class="gate-title"><%= gate.title %></h5>
                                    <p class="card-text text-center">Số lượng chặng: <%= gate.stages.length %></p>
                                </div>
    
                                <!-- Stages outside gate, now vertically aligned -->
                                <% if (gate.stages.length > 0) { %>
                                    <div class="stage-list-container">
                                        <% gate.stages.forEach(stage => { %>
                                            <!-- Stage card -->
                                            <div class="stage-card">
                                                <h6 class="stage-title"><%= stage.title %></h6>
                                                <!-- Nút vào học -->
                                                <a href="/stage/detail/<%= stage._id %>" class="btn btn-primary" onclick="return confirmStageAction()">VÀO HỌC</a>
                                            </div>                                    
                                        <% }) %>
                                    </div>                                    
                                <% } else { %>
                                    <p class="text-center">Chưa có chặng nào trong cổng này.</p>
                                <% } %>
                            </div>
                        <% }) %>
                    <% } else { %>
                        <p>Chưa có Gates nào trong hành trình này.</p>
                    <% } %>
                </div>
            </div>
    
            <!-- Right Panel: Leaderboard -->
            <div class="col-md-3">
                <div class="leaderboard-panel">
                    <h4>Bảng xếp hạng</h4>
                    <table class="leaderboard-table">
                        <thead>
                            <tr>
                                <th>Người học</th>
                                <th>Điểm KN</th>
                            </tr>
                        </thead>
                        <tbody>
                            <tr>
                                <td>Đinh Cao Toàn</td>
                                <td>2500</td>
                            </tr>
                            <tr>
                                <td>Lê Dương Chí Bảo</td>
                                <td>2400</td>
                            </tr>
                            <tr>
                                <td>Phạm Tuấn Kiệt</td>
                                <td>2300</td>
                            </tr>
                            <tr>
                                <td>Tô Hoàng Tuấn</td>
                                <td>2200</td>
                            </tr>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
    <%- include('../partials/footer.ejs') %>
    <script>
        function confirmStageAction() {
            return confirm('Bạn có muốn làm chặng này không?');
        }
    </script>
    <script src="/static/js/jquery-1.12.1.min.js"></script>
    <script src="/static/js/popper.min.js"></script>
    <script src="/static/js/bootstrap.min.js"></script>
    <script src="/static/js/jquery.magnific-popup.js"></script>
    <script src="/static/js/swiper.min.js"></script>
    <script src="/static/js/masonry.pkgd.js"></script>
    <script src="/static/js/owl.carousel.min.js"></script>
    <script src="/static/js/jquery.nice-select.min.js"></script>
    <script src="/static/js/slick.min.js"></script>
    <script src="/static/js/jquery.counterup.min.js"></script>
    <script src="/static/js/waypoints.min.js"></script>
    <script src="/static/js/custom.js"></script>
</body>
</html>
