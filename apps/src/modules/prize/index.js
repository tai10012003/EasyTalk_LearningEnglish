const { createPrizeController } = require('./controllers/prizeController');
const PrizeService = require('./services/prizeService');

module.exports = {
    createPrizeController,
    PrizeService
};
