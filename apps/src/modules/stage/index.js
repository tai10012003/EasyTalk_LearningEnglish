const { createStageController } = require('./controllers/stageController');
const StageService = require('./services/stageService');

module.exports = {
    createStageController,
    StageService
};
